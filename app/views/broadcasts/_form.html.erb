<%= form_with(model: broadcast) do |form| %>
  <% if broadcast.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(broadcast.errors.count, "error") %> prohibited this broadcast from being saved:</h2>

      <ul>
        <% broadcast.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date, class: "form-label" %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :program_number, class: "form-label" %>
    <%= form.number_field :program_number, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
